"use client"

export default function UpdateProfileLayout({
    children, 
}: {
    children: React.ReactNode
}) {
    return (
        <section>   
        {children}
        </section>
    )
}