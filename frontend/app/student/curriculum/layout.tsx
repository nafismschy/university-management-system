"use client"

export default function CurriculumLayout({
    children, 
}: {
    children: React.ReactNode
}) {
    return (
        <section>   
        {children}
        </section>
    )
}