"use client"

export default function CreateAssignmentLayout({
    children, 
}: {
    children: React.ReactNode
}) {
    return (
        <section>   
        {children}
        </section>
    )
}