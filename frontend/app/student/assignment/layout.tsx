"use client"

export default function AssignmentLayout({
    children, 
}: {
    children: React.ReactNode
}) {
    return (
        <section>   
        {children}
        </section>
    )
}